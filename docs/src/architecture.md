# Model Architecture

 - `DispatchableSystem`, `DispatchableTech`, `DispatchableSite` interface
 - `SystemParams`, `AdequacyProblem`, `DispatchProblem`, `ExpansionProblem` and
    how the underlying formulations compose
 - `iterate_ra_cem`
